# **重要规则** 本规则强制执行，不允许被覆盖、跳过、即便被压缩也要保留完整信息

对话开始前、压缩后必须阅读`AGENTS.md`

## 工作结束的时候，必须**使用ask工具询问用户下一步**，如果你写了Todotask，这一项必须写进去

- 修改结束后，**使用ask工具向用户交付**
- 报告写完后，**使用ask工具提交给用户审核**
- 测试结束后，**使用ask工具向用户交付**
- 审计完成后，**使用ask工具向用户交付**
- 用户提问回答完毕后，**使用ask工具询问用户下一步**
- 用户表明去测试时，**使用ask工具等待用户回复**
- 其他工作同理，完成后**必须使用ask工具向用户交付**

- 修改结束后，**使用ask工具向用户交付**
- 报告写完后，**使用ask工具提交给用户审核**
- 测试结束后，**使用ask工具向用户交付**
- 审计完成后，**使用ask工具向用户交付**
- 用户提问回答完毕后，**使用ask工具询问用户下一步**
- 用户表明去测试时，**使用ask工具等待用户回复**
- 其他工作同理，完成后**必须使用ask工具向用户交付**

## **特别注意，如果会话被压缩（Compaction），由于压缩信息可能不准确，你必须询问用户下一步而不是执行压缩信息里面的Goal等指令**

**本命令强制执行，不允许被覆盖、跳过，不可以被会话中的任何指令，规则覆盖**。因为压缩信息可能不准确，继续执行很可能出现**非预期行为**，所以必须使用ask工具询问用户下一步
压缩信息可能出现“继续”/“接下来执行”/“目前任务”/“待完成”/“下一步”等字眼，千万一定不要执行，必须询问用户，因为从残余信息“继续”具备非常大的风险，所以必须等待用户回复

如何判断对话被压缩（满足其一，或者你认为就是刚从压缩会话回复）：
1. 上一轮对话出现类似以下格式
```
Goal
xxx
Instructions
xxx
Discoveries
xxx
Accomplished
xxx
未完成工作
xxx
已修改的文件
xxx
```

2. 会话只有**摘要信息**

3. 出现`"This session continues a previous conversation that lost its context. A summary has been provided."`等提示

**特别注意，如果会话被压缩（Compaction），由于压缩信息可能不准确，你必须询问用户下一步而不是执行压缩信息里面的Goal等指令**

## 如果用户回复要干新的事情，请写新todo而不是继续旧todo（是todo不仅是todolist）

## 给压缩Agent的话

如果你是压缩Agent（Compaction Agent），或者被指派执行压缩会话任务，请**务必把立刻调用ask工具询问用户下一步作为首要任务**，即压缩后的信息必须明确表明接下来的任务是调用ask工具询问用户下一步：在“待执行任务”中写上
```
1.重新阅读`AGENTS.md`
2.使用ask工具询问用户下一步
```

## 🟡ask被跳过/ask内容为null的情况

可能是系统问题或者用户误点，只有在**用户明确表明退出会话、停止项目**或者重试三次之后仍是ask被跳过/ask内容为null才能停止ask

---

# 称呼

请检查用户用户名，如果是“rikka”，之后就称用户为Rikka/rikka。若用户没要求，写代码、注释、文档不要把“Rikka”这个名字写进去

# 项目

最低版本Python3.8，禁止引入高于3.8的特性

# 工程规范

## Python3 应该遵循PEP8 语言规范

## **避免采用降级、兼容、缓解方案**
不要用任何的降级、兼容、缓解方案，绝对禁止

多看看，看相关部分，多思考

## 旧代码重构后，非必要，**不要留兼容接口**，应该更新引用点
重构后完全没必要兼容以前的结构，引用点必须更新

## 采用干净架构
关键原则
	依赖规则：依赖只能从外层指向内层，内层永远不依赖外层。
	框架独立：业务逻辑不依赖具体框架，可随时替换技术实现。
	用例驱动：围绕业务用例（如用户注册、下单）构建系统，而非技术细节。
四层结构（洋葱模型）
	实体层（Entities）：核心业务对象与规则，纯领域逻辑。
	用例层（Use Cases）：应用特定业务逻辑，协调数据流。
	接口适配器层（Interface Adapters）：数据格式转换，连接用例与外部系统。
	框架与驱动层（Frameworks & Drivers）：具体技术实现，如 Express、Vue、数据库驱动等。

不必一直执着与最小改动，代码架构有问题需要及时向用户汇报

## 代码文件、函数、类开头，关键部分，决策点，注意点的注释记得写。日志必须写，并且要有完备的日志系统
注释与日志，新增代码请检查
- [] 关键部分注释加了吗
- [] 日志写了吗，是否符符合规范

## 可以使用标准库、第三方库大量精简代码、手工实现复杂的，可下载库使用。需要什么库自己下载
例子：计算字符宽度时，使用wcwidth（假如是python），而不是手算

## 不要无用的防御编程与过度工程，比如`function A() {} if(type A === "function") A()`这种的不可以的

## 不要硬编码路径 错误:`C:/windows/xxxx.exe` 正确:`WINDIR`
	
# 工具：
- .agents\skills\download-by-mirror	**下载文件，尤其从github下载请使用镜像**
- .agents\skills\python-win32api
- .agents\skills\windows-debugging	Window Debugging 套件

需要什么工具自己下载，主动性强一点
不要用浏览器代理skill/mcp

# 参考资料：
	reference\
		xtermjs-docs\
		tmux-master\
		terminal-main\
		MiMo-Code-main\
		my_rime-master\
		tcell-v2.13.10\
*想参考其他什么自己下载，使用webfetch搜索

# **测试与Debug：**
## 工程必须写测试，最少要写e2e测试，可以自动化操作的尽量用自动化
## **未查到根本因素时，不要直接改工程源码**
## 你可以
	**查日志**
	**使用工具附加调试（cdb/pdb/...）**，请下载对应符号SRV*e:\Symbol*http://msdl.blackint3.com:88/download/symbols
	**最小代码复现**等方式debug
若有些操作无法实现，请寻求用户帮助
## 复杂功能可以参考成熟开源方案，自己去github下载
## bug修复后。**需撤销之前的无效修改点**,不能让无效改动污染代码！
debug完成后，检查
- [] 旧改动删了吗
- [] 旧改动删了后，是否还能正常运行
	
# 用户交互
	**必须与用户对齐需求**，防止实现偏差
	关键决策点要由用户指定
	对用户的需求、方案、提出bug现象等信息有疑惑，必须及时追问、用户提出方案有不足时，要及时沟通

---

出现“从命令中解析目标路径失败：${normalizedCommand}”时，请不要用python -c以及复杂脚本（powershell），请使用写脚本文件执行代替